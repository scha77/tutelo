- "proxy.ts uses getClaims() not getSession() — post-Nov 2025 Supabase auth-js pattern"
- "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY not ANON_KEY — required for post-Nov 2025 Supabase projects"
- "Wave 0 test stubs use it.todo() (not it.skip) — produces green vitest run without false confidence"
- "sonner used for toast notifications (shadcn/ui v4 replaced toast with sonner)"
- "src/middleware.ts required as Next.js 16.1.6 entry point — proxy.ts is invoked from middleware.ts, not used directly as middleware filename"
- "OAuth callback wraps teachers table query in try/catch defaulting to /onboarding — resilient until Plan 01-03 creates the schema"
- "Google OAuth redirect uses window.location.origin — dynamic, works across dev/staging/prod without env var"
- "LoginForm uses two-mode (signin/signup) toggle rather than separate pages — reduces nav complexity"
- "availability.start_time and end_time use TIME (no tz) — interpreted relative to teachers.timezone (IANA string); correct pattern for recurring weekly slots"
- "bookings.teacher_id has no CASCADE on delete — preserves booking history if teacher account deleted"
- "bookings_anon_insert policy uses WITH CHECK (true) — Phase 2 tightens this; accepting loose policy now to unblock guest booking flow"
- "profile-images bucket config in supabase/config.toml mirrors remote bucket; remote bucket requires manual Dashboard creation (Supabase SQL cannot create storage buckets)"
- "reviews.parent_id is nullable — allows for future guest reviewer flow in Phase 5"
- "Insert-then-update instead of upsert: Supabase upsert with partial data triggers NOT NULL constraint violations on required columns not yet filled"
- "Auto-slug inserted on first teacher row creation inside signIn action, not in onboarding step 3 — ensures slug exists early for collision resolution"
- "teachers.user_id UNIQUE constraint added via migration — required for insert-then-update pattern to work correctly"
- "Zod v4 uses .issues not .errors on ZodError — zodResolver cast needed for coerce type mismatch"
- "Sonner Toaster added to root layout for Server Action error toasts"
- "Fixed reference date (2025-01-13 Monday) in timezone conversion avoids flakiness from 'current date' math"
- "isDraftPage/shouldShowDraft extracted to src/lib/utils/profile.ts for unit testability without RSC server context"
- "AvailabilityEditor uses delete-then-insert pattern for availability slot replacement (same as Server Action)"
- "Dashboard is desktop-first for MVP — Sidebar hidden on mobile (responsive is Phase 2+ enhancement)"
- "Banner upload uses uploadBannerImage Server Action with FormData (not URL string) — needed because Server Actions can't receive File objects directly via JSON"
- "AvailabilityGrid normalizes DB time strings: Postgres TIME columns return HH:MM:SS, must strip to HH:MM before timezone conversion"
- "Badge uses href comparison ('/dashboard/requests') not label string for coupling safety"
- "Stripe warning banner is conditional layout block, not a separate notification system"
- "pendingCount fetched sequentially after teacher (needs teacher.id), not truly parallel"
- "email.ts created as real Resend implementation (overwritten from stub by external 02-03 TDD process)"
- "vi.mock('@/lib/email') in test file prevents Vite import analysis error for dynamic import"
- "Pre-existing email.test.ts failures (3 tests) from 02-03 RED phase are out of scope — logged to deferred-items"
- "vi.hoisted() + class-based MockResend required for Vitest ESM mocking of `new Resend()` — vi.fn().mockImplementation() is not a constructor in Vitest's SSR transform"
- "Module-level `const resend = new Resend(...)` works with class-based mock — no lazy-init needed"
- "RESEND_API_KEY documented in .env.local as placeholder — team must provision real key before email delivery works"
- "supabaseAdmin used in all three new email functions — not createClient() — because these are called from webhook handlers with no user session"
- "fire-and-forget sendSessionCompleteEmail in markSessionComplete — avoids blocking capture response on email delivery"
- "Confirmed Sessions section only rendered when confirmedBookings.length > 0 — clean empty state preserved"
- "booking-confirmed page falls through to generic 'Your booking is confirmed' if Stripe session retrieval fails — resilient to session expiry"
- "review URL stub /review?booking=bookingId in SessionCompleteEmail — Phase 5 implements actual review flow; link included so parent can find it after launch"
- "Chain .select('id') on Supabase JS v2 .update() calls to get affected rows — count property is always null without { count: 'exact' } option"
- "reminder_sent_at NULL = unsent (cron filters IS NULL), set to NOW() on dispatch — simpler than boolean flag, enables timestamp audit"
- "Partial index scoped to confirmed + unsent only — keeps index small as completed/cancelled bookings never need reminders"
- "it.todo() stubs for all 7 Wave-1 test files — Wave 2 plans (02, 03, 04) can run in parallel without schema or test file gaps"
- "Destination charges without on_behalf_of: platform-side PaymentIntent with transfer_data.destination only — avoids Stripe error with on_behalf_of on destination charges"
- "Orphan booking cleanup: delete booking row if Stripe PI creation fails so slot is not permanently blocked"
- "loadStripe at module level in PaymentStep — per Stripe docs, prevents re-initialization performance penalty"
- "Inline Supabase auth (client SDK) not Server Action — Server Actions redirect away, breaking booking state"
- "Google OAuth accepted limitation: redirects away from booking flow — booking state lost (MVP tradeoff)"
- "accountUrl passed to parent email only, not teacher email — teacher uses /dashboard instead"
- "redirectTo passed via FormData to server action — keeps LoginForm as 'use client' without router dependency in auth actions"
- "Pure logic tests for RSC: splitBookings and getInitialSubject extracted inline in test files to avoid full RSC render"
- "proxy.ts carries the auth/routing logic, not middleware.ts — middleware.ts is just a re-export shim"
- "Cron schedule 0 9 * * * (9 AM UTC) covers both US coasts for same-day tomorrow date boundary"
- "Parent receives recipientFirstName='there' — parent name not collected at MVP"
- "Teacher email gated on social_email != null — same pattern as all other teacher notification emails"
- "sumEarnings and groupStudents extracted as inline pure functions in test file — avoids RSC render context in tests"
- "Past sessions use div grid layout (not HTML table) per plan spec"
- "reviews relation typed as Array<{rating: number | null}> | null to handle Supabase join type"
- "Pre-existing REVIEW-02 test expectation fixed: JSX renders review count as children array, not plain string"
- ".select('id') + if (teacher) check existence not publication status — any teacher row redirects to /dashboard"
- "Query param must precede hash fragment in URL: ?subject=X#booking not #booking?subject=X"
- "BookNowCTA import was already present — only JSX render insertion needed"
- ".in('status', ['requested', 'pending']) used in both createCheckoutSessionsForTeacher and checkout.session.completed — covers teacher-accept-before-Stripe scenario without changing direct booking path"
- "payment_intent.amount_capturable_updated handler left unchanged — its .eq('status', 'requested') guard is correct for the direct booking flow where pending is never an intermediate state"
- "sendBookingConfirmationEmail added to @/lib/email mock in test file to support checkout.session.completed test assertions"
- "middleware.ts → proxy.ts rename: Next.js 16 deprecated middleware convention; proxy.ts with export function proxy() is the new standard. proxy runtime is nodejs (not edge)."
- "Dashboard layout auth uses getUser() not getClaims() — getClaims() reads unverified cookies that fail on server-action POST re-renders; getUser() makes verified API call to Supabase Auth"
- "Proxy handles token refresh only (getUser call), no auth redirect — auth protection delegated to layouts/pages to avoid server-action POST re-render cookie issues"
- "Server-action auth bug identified: Next.js 16 does not forward cookies correctly during layout re-render triggered by server action POST. Affects all server actions using redirect() under dashboard layout. Fix: convert to API route handler pattern."
- "connectStripe converted to POST /api/connect-stripe API route handler — server actions under dashboard layout fail auth on Next.js 16 POST re-renders; API route handlers correctly receive forwarded cookies. Client component calls fetch() and redirects via window.location.href."
- "Brand palette: primary #3b4d3e (sage green), secondary #f6f5f0 (warm off-white). Applies to app chrome and landing page. Does NOT override teacher accent colors on profile pages (PAGE-07 preserved)."
- "Landing page is teacher-only audience — parents arrive via teacher slug links, not the homepage. No parent-facing sections on landing page."
- "Landing page tone: sleek & modern (clean startup feel like Stripe/Linear). Hero leads with professional identity + zero risk messaging."
- "Animation style: subtle & smooth — gentle fade-ins, soft slide-ups, smooth transitions. Not bouncy, playful, or dramatic."
- "Mobile dashboard uses bottom tab bar (not hamburger menu) — thumb-friendly, app-like feel."
- "social_email auto-populated from auth signup email — zero friction, teacher can change later in settings. Fixes silent notification gap."
- "Dynamic OG tags per teacher /[slug] page — personalized previews when links shared on social/email/newsletters."
- "Landing page CTA is 'Start your page' → direct to signup. No demo page intermediate step."
- "Founding Teacher badge NOT on landing page yet — saved for Instagram launch. Landing page kept universal."
- "Logo file: public/logo.png (Tutelo 'T' with leaf motif, 4000x4000). Needs optimized web variants."
- "Landing page sections extracted to src/components/landing/ as separate RSC components — enables S02 animation wiring without modifying page.tsx"
- "TeacherMockSection is the only 'use client' landing component — all others are server components for build-time rendering"
- "S01 uses only tw-animate-css and CSS transitions for interactivity — motion library installation deferred to S02"
- "Import motion from 'motion/react-client' (not 'motion/react') in App Router client components — tree-shaken client-only build reduces bundle size"
- "Thin animated wrapper pattern for RSC→animation: AnimatedSection, AnimatedProfile, AnimatedList are 'use client' wrappers that accept RSC children — keeps data-fetching in RSCs and minimizes client boundary footprint"
- "template.tsx (not layout.tsx) for page transitions — template remounts on every App Router navigation, giving AnimatePresence the key change it needs; layout persists (sidebar stays stable)"
- "Animation constants centralized in src/lib/animation.ts — all variants, durations, easings, and viewport config shared across all surfaces for consistency"
- "AnimatePresence imported from 'motion/react' (not 'motion/react-client') — motion v12 only exports HTML element factories from react-client; orchestration components like AnimatePresence live in the main react entrypoint"
- "navItems extracted to src/lib/nav.ts (shared between Sidebar and MobileBottomNav) — single source of truth prevents nav item drift between desktop and mobile"
- "MobileBottomNav uses icon-only layout for all 7 tabs at 375px — ~53px per tab is tight but workable; labels omitted to prevent overflow"
- "MobileHeader is a separate component from MobileBottomNav — header shows logo+name (top), nav shows tabs (bottom); cleaner separation than combining into one component"
- "opengraph-image.tsx uses direct @supabase/supabase-js createClient (not server.ts createClient) — edge runtime cannot access cookies() from next/headers; anonymous public read is sufficient for OG image generation"
- "generateMetadata() in [slug]/page.tsx shares the same Supabase query pattern as the page component — Next.js deduplicates within the same request lifecycle; no caching optimization needed"
- "social_email auto-population uses getUser() not getClaims() — getUser() makes verified API call, works for both email/password and Google OAuth, consistent with dashboard layout auth pattern"
- "Separate availability_overrides table (not nullable specific_date column on availability) — keeps recurring and override query paths separate, avoids nullable-primary-key confusion, makes migration additive and zero-risk to existing data"
- "Time-range picker paradigm (start/end per day) instead of 5-min toggle grid — 288 toggles per day is unusable; teachers think in ranges ('3:30–5:00'), not individual slots"
- "Override-wins-recurring precedence: if ANY override row exists for a specific date, use ONLY override rows for that date, ignore recurring — prevents ambiguous merge of two availability sources"
- "Booking calendar presents slots at 30-min booking increments within availability windows — balances 5-min availability precision with parent-facing usability (no 36-button slot list for a 3-hour window)"
- "S04 (cancellation) is independent of S01–S03 (availability) — can be built in parallel; no shared schema or component dependencies"
- "Multiple time windows per day supported in editor — teachers may have split schedules (8–11 AM + 3–5 PM); each day gets a list of non-overlapping ranges"
- "Time utilities extracted to src/lib/utils/time.ts — generate5MinOptions, formatTimeLabel, validateNoOverlap are pure functions reused by recurring editor (S01) and override editor (S02)"
- "Select dropdowns grouped by hour for 288 time options — SelectGroup + SelectLabel headers ('8 AM', '9 AM') to improve usability over flat 288-item list"
- "availability_overrides migration shipped in S01 (not S02) — additive table creation is zero-risk, gets schema work out of the way before editor work"
- "Editor state is Map<number, {start_time, end_time}[]> — grouped by day_of_week, replacing the Set<string> toggle key pattern from the old 1-hour grid"
- "computeSessionAmount extracted to src/lib/utils/booking.ts as pure function — decouples payment math from route handler for testability; create-intent imports it"
- "Payment proration: create-intent computes amountInCents from (endTime - startTime) / 60 * hourlyRate * 100 — no longer assumes 1-hour sessions; 30-min slot costs half the hourly rate"
- "cancelSession calls existing sendCancellationEmail (both parties emailed) rather than forking a parent-only variant — accepts teacher gets stale 'expired' copy until CancellationEmail gets a reason prop in a follow-up"
- "cancelSession uses window.confirm for destructive action confirmation — lightweight, no extra component dependency; sufficient for MVP"
- "Stripe PI cancel failure is non-blocking — try/catch logs error, DB still updates to cancelled; authorization auto-expires after 7 days as Stripe fallback"
- "SMS track before verification track in M005 — SMS is higher confidence, higher parent/teacher impact, and technically straightforward (mirrors existing email.ts pattern). Verification has more unknowns."
- "School email domain check for teacher verification (not manual founder review, not third-party API, not state license lookup) — school email is free, automatable, meaningfully trustworthy, and avoids 50-state fragmentation. Alumni .edu addresses are accepted — 'meaningful but not bulletproof' trust is appropriate for MVP."
- "Twilio for SMS provider (not MessageBird, not Vonage) — industry standard, built-in STOP/HELP keyword handling, Node SDK mirrors Resend pattern, $0.0079/segment US pricing acceptable at MVP scale"
- "sms_opt_in on teachers table for teacher SMS; parent_phone + parent_sms_opt_in on bookings table for parent SMS — parents on deferred path have no persistent account, so consent is per-booking"
- "Single migration 0008 for all M005 schema changes — teachers gets phone_number, sms_opt_in, verified_at; bookings gets parent_phone, parent_sms_opt_in. Additive columns only, no breaking changes."
- "SMS reminder extends existing session-reminders cron (not a separate cron) — avoids duplicate bookings queries and stays within Vercel Hobby cron limits"
- "Cancellation SMS fires synchronously in cancelSession request (fire-and-forget with .catch), not via cron — last-minute cancellations need immediate delivery"
- "Custom verification token (not Supabase magic link) for school email verification — separates auth from verification, avoids confusion when teacher's auth email differs from school email"
- "CredentialsBar badge gated on verified_at IS NOT NULL — fixes pre-existing trust liability where every teacher shows as 'Verified' regardless of actual verification status"
- "Phone number restricted to US (+1) only at MVP — TCPA applies to US numbers; international rules differ and add complexity"
- "libphonenumber-js for phone validation (not regex) — handles formatting, E.164 conversion, and edge cases correctly"

---

## Decisions Table

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M007/S02/T01 | convention | How checkAndNotifyWaitlist is called from cancelSession | Dynamic import + fire-and-forget with .catch(console.error), matching existing sendCancellationEmail and sendSmsCancellation patterns in the same function | Notification failures must never block or fail the cancellation flow itself. The dynamic import keeps the waitlist module out of the cancelSession bundle path. The .catch(console.error) pattern is already established for email and SMS sends in the same function — consistency reduces cognitive load. | Yes | agent |
| D002 | M007/S02/T01 | architecture | When to stamp notified_at on waitlist entries during notification | Batch-stamp notified_at only on successfully-sent IDs via a notifiedIds accumulator, in a single DB call after all emails attempted | Per-entry stamping would require N DB calls. Stamping all before sending would mark entries as notified even if the email failed. Accumulating successful IDs and batch-stamping ensures only successfully-notified parents are marked, while minimizing DB round trips. | Yes | agent |
| D003 | M007 planning | architecture | How session type selector integrates into BookingCalendar step machine | Session type selector shown as a conditional block at the top of the calendar step, not as a new named step in the step machine | Adding a new named step ('session_type') to the 6-step state machine increases branching complexity and requires new back-navigation logic. A conditional block within the existing 'calendar' step — shown when session types exist and none is selected yet — minimizes state machine changes. The session type selection state is just another form field (selectedSessionType), and once selected, the calendar renders with filtered slots. The user can change their session type selection without navigating between named steps. | Yes | agent |
| D004 | M007/S01/T03 | architecture | How anonymous (unauthenticated) parent mutations are handled for waitlist signup | API route (/api/waitlist POST) with supabaseAdmin service role, rather than a server action | Server actions require a request context compatible with Next.js auth patterns, but parent waitlist signups are anonymous — no session exists. An API route with supabaseAdmin bypasses RLS cleanly and avoids the complexity of passing anon-safe supabase clients through server action auth flows. The pattern also makes error responses (201/409/400/500) explicit and testable vs. the opaque return value of server actions. | Yes | agent |
| D005 | M007/S01/T01 | architecture | Capacity utility design: single function vs. split pure-logic + async-query | Pure isAtCapacity() function + async getCapacityStatus(supabase, teacherId, capacityLimit) function as two separate exports | Splitting into a pure isAtCapacity() and an async getCapacityStatus() enables unit testing of capacity logic without Supabase mocking while keeping the DB query layer clean. The async function also short-circuits (no DB query) when capacityLimit is null, which is the common case for teachers with no limit set. | Yes | agent |
| D006 | M007/S02 | requirement | WAIT-03 status transition | validated | M007/S02 delivered: checkAndNotifyWaitlist(teacherId) utility rechecks capacity via getCapacityStatus after cancellation, fetches unnotified waitlist entries, sends WaitlistNotificationEmail per-entry with individual try/catch, batch-stamps notified_at on notifiedIds accumulator; cancelSession invokes it as fire-and-forget via dynamic import + .catch(console.error); 7 unit tests + 1 cancel-session integration test all pass (16 total). | Yes | agent |
| D007 | M007/S02 | convention | Zod ZodError property access pattern for safeParse results | Use parsed.error.issues[0].message (not .errors) in Zod safeParse error handling | Zod v3 ZodError exposes .issues (array of ZodIssue), not .errors. TypeScript correctly flags .errors as non-existent. Discovered during S02 build verification when pre-existing S03 session-types.ts code caused build failure. Fixed in session-types.ts as a blocker-adjacent fix during S02 close. | Yes | agent |
| D008 | M007/S03 | architecture | Whether to add session_type_id column to bookings table for S03 | Store session_type_id in Stripe PaymentIntent metadata only; skip adding a bookings table column | S03 is about getting the right price through Stripe, not full DB normalization. The session type label is already captured in the booking's subject field. Adding a column requires a new migration (0012) that delivers no user-facing value at MVP. If analytics needs arise later, an additive migration is trivial. The deferred path (non-Stripe teachers) doesn't need session type pricing at all — session type label as subject is sufficient. | Yes — add 0012_bookings_session_type_id.sql if reporting/analytics require FK-based queries | agent |
| D009 | M007/S03 | requirement | SESS-02 status transition | validated | M007/S03 delivered: profile page fetches session_types and passes them to BookingCalendar; BookingCalendar renders a session type picker panel (label, price, duration cards) as a calendar-step guard when session types exist; selectedSessionType state drives price display in form header ($XX · Label); 8 session-type-pricing tests + 18 booking-slots tests all pass. | Yes | agent |
| D010 | M007/S03 | requirement | SESS-04 status transition | validated | M007/S03 delivered: when sessionTypes prop is empty/absent, BookingCalendar skips session type selector, shows subject dropdown as before, and create-intent route uses existing computeSessionAmount hourly_rate path unchanged; backward compat unit test in session-type-pricing.test.ts passes. | Yes | agent |
| D011 | M009 | architecture | Payment timing model for recurring sessions | Charge 24h before each session using saved card (Stripe Customer + SetupIntent), not bulk upfront or weekly batch | Stripe authorizations expire after 7 days. An 8-week series can't pre-authorize all sessions. Per-session auto-charge via saved card is fairest (parent not locked in), simplest for refunds, and matches subscription service UX expectations. | Yes — if parents complain about per-session charges, could offer upfront payment option | collaborative |
| D012 | M009 | architecture | Conflict handling for partial recurring series | Create sessions for available dates, skip conflicts, show parent which dates were skipped and why | A teacher having one day off in 8 weeks shouldn't block the entire recurring relationship. Partial series creation with a clear summary is the most flexible approach — parent can see exactly what they're getting. | No | collaborative |
| D013 | M009 | architecture | Cron strategy for recurring session charges | Extend existing /api/cron/stripe-reminders, not a separate cron job | Vercel Hobby plan has limited cron slots. Fewer moving parts. Query pattern is nearly identical to existing reminder logic (find tomorrow's sessions). Adding a second cron doubles the bookings table query load without benefit. | Yes — if cron execution time exceeds Vercel limits, split into separate jobs | collaborative |
| D014 | M009 | convention | Dashboard recurring session display pattern | Unified session list with series badge and cancel-series action — no separate recurring management page | A separate management page adds navigation complexity without much value at current scale. Series badge + cancel action in the existing session list keeps the mental model simple. Teachers already know where their sessions live. | Yes — if teachers find the unified list too noisy with many recurring series | collaborative |
| D015 | M009 | architecture | Who can cancel recurring sessions | Both teacher (from dashboard) and parent (via secure email link / /manage/[token] page) can cancel | Parents need agency over their recurring commitment without needing to contact the teacher for every cancellation. Secure token-based link follows the established review token pattern and matches how subscription services handle self-service cancellation. | No | collaborative |
| D016 | M009/S01 | architecture | Stripe card-saving approach for recurring first session | Use PaymentIntent with `setup_future_usage: 'off_session'` on the first session PI, not a separate SetupIntent + PI two-step flow | A single PI with setup_future_usage saves the card to the Stripe Customer automatically when confirmed. This halves browser round-trips (one confirmation instead of two), reuses the existing PaymentStep component unchanged, and Stripe confirms this is compatible with capture_method: 'manual'. If incompatible at runtime, the fallback is a two-step SetupIntent flow, but Stripe docs and the research spike indicate compatibility. | Yes — if setup_future_usage proves incompatible with manual capture in production, switch to two-step SetupIntent flow | agent |
| D017 | M009/S01 | architecture | Conflict pre-check architecture for recurring booking UI | Add a lightweight `/api/direct-booking/check-conflicts` endpoint that calls checkDateConflicts and returns available/skipped dates, rather than doing conflict detection purely client-side or bundling it into the create-recurring route | The RecurringOptions component needs to show projected dates with availability status before the parent commits. Conflict detection requires querying existing bookings and teacher availability — these are server-side queries. A dedicated pre-check endpoint keeps the UI responsive (shows preview as parent adjusts count/frequency) and separates the read-only check from the write-heavy create-recurring route. The check is called on frequency/count changes; create-recurring re-checks at insert time for race conditions. | Yes — could be inlined into create-recurring if the pre-check proves unnecessary | agent |
| D018 | M009/S01/T01 | architecture | Date arithmetic strategy for recurring session generation | UTC noon anchor (new Date(`${dateStr}T12:00:00Z`)) for all date arithmetic in generateRecurringDates | DST transitions can shift midnight-anchored dates by one day depending on the system timezone. Anchoring at UTC noon avoids this: +7 or +14 days in milliseconds always lands on the correct calendar date regardless of DST gaps. A test crossing a DST boundary (e.g., March → November) caught this during T01 development. | Yes | agent |
| D019 | M009/S01/T02 | architecture | Booking row insertion strategy for recurring series creation | Per-row sequential inserts with 23505 (unique constraint violation) skip handler, not a single batch INSERT ... ON CONFLICT DO NOTHING | Per-row insert with 23505 skip allows partial success when race conditions claim individual slots between conflict-check and insert. A batch ON CONFLICT DO NOTHING would silently discard all rows or succeed atomically — neither gives the caller which specific rows were skipped. Per-row also allows fine-grained error reporting in skippedDates response. | Yes | agent |
| D020 | M009/S01/T02 | architecture | Stripe Customer creation lifecycle for recurring bookings | New Stripe Customer per recurring booking at booking-creation time; customer ID stored in recurring_schedules for S02 auto-charge use | Creating a new customer per booking keeps the first-session flow simple and identical to the one-time booking flow. Customer reuse (looking up existing customers by email) adds latency and Stripe API complexity for S01 scope. The customer ID is stored on recurring_schedules so S02 can retrieve it for the auto-charge cron without touching Stripe's customer list. Deduplication can be added in a later milestone. | Yes | agent |
| D021 | M009/S02 | M009/S02 | Cron strategy for recurring session charges — override D013 | Dedicated `/api/cron/recurring-charges` route at `0 12 * * *` instead of extending existing `/api/cron/stripe-reminders` | D013 chose extending stripe-reminders because "Vercel Hobby plan has limited cron slots." Since Jan 2026, Vercel lifted the per-project cron limit to 100 on all plans. A dedicated route is cleaner: avoids coupling charge logic with reminder logic, enables independent scheduling (noon UTC gives 2h buffer before session-reminders at 14:00 UTC), and keeps each cron route single-purpose. The stripe-reminders route has a "requires Pro" comment and different query patterns — merging charge logic into it would add complexity without benefit. | No — D013 is superseded | agent |
| D022 | M009/S03 | M009/S03 | Cancel token security model for parent self-service cancellation | Non-expiring 64-char hex cancel_token stored on recurring_schedules, generated at booking creation. Token-based auth (no login required) for /manage/[token] page and /api/manage/* routes. Token included only in parent confirmation email, never in teacher email or dashboard UI. | Recurring series can span 6+ months — expiring tokens would lock parents out. The 256-bit random token (randomBytes(32)) is cryptographically secure and unguessable. Token-only auth follows the established review/[token] pattern and matches how subscription services handle self-service cancellation. The token is effectively the parent's credential for this specific series — scoped to a single recurring_schedule_id, not a general auth token. | Yes — could add expiry or rate-limiting if abuse is detected | agent |
| D023 | M010 planning | architecture | Saved payment method scope per parent | Single saved card per parent (auto-saved on first Stripe booking), not multiple cards with selector | Multiple saved cards adds a management UI (card picker, set default, remove individual cards) that most parents won't need. Single card covers the primary use case — rebooking friction reduction. Parent can update their card from the dashboard. | Yes — add multi-card if parent feedback requests it | collaborative |
| D024 | M010/S01/T02 | architecture | Children CRUD approach: client-side Supabase vs. API routes | Client-side Supabase with RLS for children CRUD (no dedicated API route needed in parent dashboard children page) | RLS policy `children_owner_all` enforces parent_id = auth.uid() ownership at the DB level. The parent dashboard children page is a 'use client' component that calls the Supabase JS client directly. This eliminates an API route layer while still being secure. The separate /api/parent/children route exists for BookingCalendar (which can't use server-side Supabase), but the dashboard CRUD uses the client directly. | Yes | agent |
| D025 | M010/S01/T01 | convention | Auth check pattern for teacher-vs-parent routing | getUser() (not getClaims()) consistently across all three auth paths: callback route, signIn action, and login page redirect | getClaims() is unreliable on POST re-renders in Next.js 16 (noted in PROJECT.md architecture section). Using getUser() + maybeSingle() teacher lookup gives consistent, verified identity checks. Also, callback route switched from getClaims() to getUser() for this same reason. The pattern: getUser() → if no user redirect to login; query teachers table with maybeSingle() → if teacher row exists go to /dashboard, else go to /parent. | Yes | agent |
| D026 | M010/S01/T02 | convention | TypeScript typing for Supabase relation joins | Supabase join relations return arrays (not objects) in TypeScript types — BookingRow must type children and teachers as arrays | When using Supabase `.select('*, children(*), teachers(*)')`, the returned TypeScript types reflect arrays for one-to-many joins even when a single row is expected. Using `children[0]?.name` instead of `children?.name` is the correct access pattern. This caused a build error that was fixed in T02 — future tasks that join children or teachers on bookings must account for this. | Yes | agent |
| D027 | M010/S01 | requirement | PARENT-04 status transition | validated | M010/S01 delivered: children table with RLS + child_id FK on bookings (migration 0017). /api/parent/children CRUD API (GET/POST/PUT/DELETE) with ownership verification. /parent/children page with inline add/edit/delete. BookingCalendar shows child dropdown for logged-in parents with children; falls back to text input for guests. 16 children CRUD unit tests + 15 child selector tests all passing. | Yes | agent |
| D028 | M010/S01 | requirement | PARENT-07 status transition | validated | M010/S01 delivered: login-required parent dashboard at /parent with auth-guarded (parent) route group (getUser() check, redirect to /login if no session). ParentSidebar with My Children / My Bookings / Overview nav. Three pages: overview stats, children management, booking history (upcoming/past split). Dual-role users (teacher+parent) get cross-link back to /dashboard. Auth routing in callback + signIn + login page all send parent-only users to /parent. 15 auth routing unit tests passing. | Yes | agent |
| D029 | M010/S01 | requirement | PARENT-08 status transition | validated | M010/S01 delivered: BookingCalendar useEffect fetches /api/parent/children on mount. If logged-in parent has children, renders Select dropdown with child options + "Someone else (type name)" fallback option. Selecting a child sets form.name = child.name and form.childId = child.id. Selecting "Someone else" clears childId and shows text Input. Guest bookings (no auth) always see text Input. childId passed through all three booking paths (createPaymentIntent, createRecurringIntent, handleSubmit deferred). BookingRequestSchema accepts optional child_id. 15 child selector unit tests passing, no regressions in 419-test suite. | Yes | agent |
| D030 | M010/S02 | convention | New Google user (no teacher row) routes to /parent, not a special onboarding flow | Accept that new-user-via-Google lands at /parent dashboard; teacher signup remains email+password → /onboarding | Adding a new-user detection branch to the callback route would complicate OAuth flow for a rare edge case. New teachers are driven through the "Start your page" CTA (email+password signup → onboarding wizard). Google SSO is primarily for returning logins. A new Google user who happens to want to teach can still sign up via the standard flow. | Yes | agent |
| D031 |  | requirement | AUTH-04 status transition | validated | M010/S02 delivered: Provider-agnostic smoke test reads src/actions/verification.ts source and asserts it uses getUser() with no provider-specific logic — no 'provider', 'google', or 'getSession().provider' references. Verification flow works regardless of auth provider (Google SSO or email+password). Test passes in full 426-test suite. | Yes | agent |
| D032 | M010/S03 planning | architecture | S03 task grouping: migration + route changes together in T01 | Bundle migration, create-intent changes, create-recurring changes, and webhook changes into a single task (T01) rather than separate tasks | The migration (parent_profiles table), both booking routes (create-intent/create-recurring), and the webhook handler are tightly coupled — the webhook reads what the routes write, both routes share the same parent_profiles lookup pattern, and all require the migration. Splitting them would create tasks that can't be independently verified. The combined task is still only 4 files and 4 steps — well within context limits. | Yes | agent |
| D033 |  | requirement | PARENT-05 status transition | validated | M010/S03 delivered: parent_profiles table (migration 0018) stores one Stripe Customer + saved PM per parent account. create-intent attaches parent Customer + setup_future_usage: 'off_session' so card is auto-saved on first booking. create-recurring reuses parent-level Customer from parent_profiles. Webhook payment_intent.amount_capturable_updated handler retrieves PM card details and upserts to parent_profiles. /parent/payment page displays saved card (brand, last4, expiry) with remove action. GET/DELETE /api/parent/payment-method API routes. 18 unit tests all passing. Full suite 444 passing, 0 failures. | Yes | agent |
| D034 |  | requirement | PARENT-09 status transition | validated | M010/S03 delivered: Stripe Customer is created per parent account (not per teacher or per recurring schedule) and stored in parent_profiles.stripe_customer_id. create-intent and create-recurring both check parent_profiles first and reuse the existing Customer when found. The saved payment method is reusable across any teacher booking. Existing recurring_schedules.stripe_customer_id remains for backward compat with cron auto-charges. 18 unit tests confirm Customer reuse behavior (no duplicate creation). Full suite 444 passing. | Yes | agent |
| D035 | M010/S04/T03 | architecture | Realtime subscription deduplication strategy in ChatWindow | Incoming Realtime INSERT events check for existing message ID and replace the optimistic placeholder if found, rather than appending a duplicate | Optimistic sends assign a temporary ID before the API confirms. When Realtime delivers the confirmed INSERT, the real UUID must replace the optimistic UUID — not append a duplicate message. Checking for ID overlap before append ensures the UI is always consistent with DB state. | Yes | agent |
| D036 | M010/S04/T02 | convention | Server component data access pattern for messaging list pages | server components for conversation list pages query supabaseAdmin directly instead of fetching internal /api/conversations | Internal API fetches from server components require cookie-forwarding and absolute URLs — fragile in Next.js. Direct supabaseAdmin queries in server components are the established project convention (matches all other pages) and avoid the fragility. The API routes remain available for client-side/external use. | Yes | agent |
| D037 | M010/S04 | requirement | MSG-02 status transition | validated | M010/S04 delivered: ChatWindow client component subscribes to Supabase Realtime postgres_changes on messages table filtered by conversation_id. New messages from any sender appear instantly without page refresh. Realtime deduplication handles optimistic-send overlap. Cleanup via supabase.removeChannel on unmount. migration 0019 adds messages table to supabase_realtime publication. | Yes | agent |
| D038 | M010/S04 | requirement | PARENT-06 status transition | validated | M010/S04 delivered: full text-only messaging between parent and teacher. One thread per teacher-parent relationship (UNIQUE constraint). Messages nav item in both parent sidebar (/parent/messages) and teacher sidebar (/dashboard/messages). Conversation list pages with last-message preview and relative timestamps. ChatWindow with Realtime subscription for live updates. Email notifications via Resend. 21 unit tests + tsc clean + 465 full-suite tests passing. | Yes | agent |
| D039 |  | requirement | ADMIN-01 status transition | validated | M010/S05 delivered: admin dashboard at /admin with stat cards for Total Teachers, Stripe Active, Published, Total Bookings, Completed Sessions, and Revenue (from completed bookings). All metrics fetched via supabaseAdmin in a single Promise.all. 9 unit tests passing. Full suite: 474 tests, 0 failures. | Yes | agent |
| D040 |  | requirement | ADMIN-02 status transition | validated | M010/S05 delivered: 15-item activity feed on admin page merges teacher signups (teachers.created_at), new bookings (bookings.created_at), and session completions (bookings.updated_at where status='completed'). Events sorted by timestamp descending, limited to 15, with type badges and relative timestamps. No new event table required — derived from existing tables. | Yes | agent |
| D041 |  | requirement | ADMIN-04 status transition | validated | M010/S05 delivered: (admin) route group layout reads ADMIN_USER_IDS env var (comma-separated user UUIDs), trims and filters whitespace. If allowlist is empty or user ID not in list, calls notFound() (404, not redirect — prevents route existence leakage). Unit tests confirm all four gating scenarios: unauthenticated → redirect /login, non-admin → 404, empty env → 404, undefined env → 404, admin → renders. 9 unit tests passing. | Yes | agent |
| D042 | M011 planning | scope | Scope fence for M011 — visual only vs. structural refactors | Include structural refactors — BookingCalendar decomposition and booking flow rework allowed | Visual polish alone would leave the 935-line BookingCalendar monolith in place. User wants both the look and the feel improved, which requires structural changes to the booking flow. | No | human |
| D043 | M011/S01 | convention | Accent-color application pattern on teacher profile components | Use color-mix(in srgb, var(--accent) 15%, transparent) with inline styles for tinted chip backgrounds — never use Tailwind bg-accent utility | The teacher profile page wraps content in a &lt;main&gt; with --accent CSS variable override. Tailwind's bg-accent resolves at build time to a static Tailwind CSS class, not to the runtime CSS variable. Using inline styles with color-mix() reads the live --accent value set by the teacher's accent_color, enabling correct per-teacher tinting. This pattern is used for subject chips in CredentialsBar and reviewer avatars in ReviewsSection. | Yes | agent |
| D044 | M011/S01 | requirement | UI-01 requirement status transition | validated | M011/S01 delivered: HeroSection upgraded with taller banner (h-44/h-64), larger avatar (h-24/h-28, border-4, shadow-lg), deeper overlap, ring-inset depth, balance/bold typography. CredentialsBar restructured into two-row layout with accent-colored subject chips (color-mix inline styles), lucide icon-paired meta items (Clock, MapPin, DollarSign), emerald verified badge pill, and right-aligned tabular-nums rate display. AboutSection gets uppercase label with accent border-l-4 and text-wrap: pretty on bio. ReviewsSection upgraded with SVG star icons, elevated cards (rounded-xl/shadow-sm/hover:shadow-md), accent-colored reviewer initial avatars, and accentColor prop wired from page.tsx. SocialLinks upgraded to pill-link styling with Tutelo attribution footer. All 474 tests pass, TypeScript clean, next build success. | Yes | agent |
| D045 | M011/S02 | architecture | BookingCalendar decomposition strategy — state ownership and component boundaries | All state (form, step, selectedDate, selectedSlot, etc.) and all async handlers (createPaymentIntent, createRecurringIntent, handleSubmit) stay in BookingCalendar orchestrator. Sub-components receive state and callbacks via props. BookingForm receives ~15 props (form, setForm, onSubmit, submitting, etc.) rather than co-locating form state. | Keeping state in the orchestrator preserves the step state machine in one place, avoids needing to lift state back up when transitioning between steps, and keeps the existing test surface unchanged — booking-child-selector.test.ts renders BookingCalendar and the new sub-components are exercised through it without needing mock updates. The prop-drilling is verbose but explicit and type-safe. | Yes | agent |
| D046 | M011/S02/T02 | architecture | CalendarGrid availability data interface | CalendarGrid accepts an isAvailable(date) callback instead of raw slot data arrays | Keeping availability logic (slot data lookups, timezone math) centralized in BookingCalendar orchestrator means CalendarGrid stays purely presentational — it just needs to know whether to render a date as available. Passing raw slot arrays would bleed domain logic into the grid and require re-fetching on every render. | Yes | agent |
| D047 | M011/S02 | requirement | UI-02 requirement status transition | validated | M011/S02 delivered: BookingCalendar 933-line monolith decomposed into 4 presentational sub-components (BookingForm 263 lines, SessionTypeSelector 48 lines, CalendarGrid 130 lines, TimeSlotsPanel 71 lines). All booking flow steps (session type, calendar, form, payment, auth, recurring, success/error) receive premium visual treatment: accent-colored chips in step headers, rounded-xl elevated cards in SessionTypeSelector, bg-muted/20 TimeSlotsPanel, rounded-xl projected dates with Repeat icon in RecurringOptions, Shield trust signal and heading in PaymentStep. All 3 booking paths (deferred, direct, recurring) continue to function. 474 tests pass, tsc clean, next build success (67 routes). | Yes | agent |
| D048 | M011/S03 | navigation | More menu interaction pattern for teacher mobile nav | Custom AnimatePresence bottom panel using motion/react-client — state in MobileBottomNav, slide-up animation (y: 100% → 0), backdrop overlay, click-backdrop-to-dismiss. No new dependencies. | Matches existing motion patterns in the codebase (OnboardingWizard uses AnimatePresence). Avoids adding vaul/sheet dependency. Gives full control over the slide-up animation and dismiss behavior. The existing motion library already supports everything needed. | Yes | agent |
| D049 | M011/S03 | requirement | UI-03 requirement status transition | validated | M011/S03 delivered: NavItem interface extended with optional description field; primaryNavItems (4: Overview/Requests/Sessions/Availability) and moreNavItems (7: Students/Waitlist/Page/Promote/Analytics/Messages/Settings) exported from nav.ts. MobileBottomNav fully rewritten with 5 labeled tabs and AnimatePresence bottom-sheet More panel (icon+label+description per item, backdrop dismissal, active-dot on More when deeper route is current, pending badge on Requests, form action={signOut}). ParentMobileNav confirmed already had visible labels on all 5 tabs + Sign Out. tsc --noEmit clean (0 errors), vitest run 474 tests pass, next build succeeds (67 pages). | Yes | agent |
| D050 | M011/S04 | convention | Dashboard chip tinting: var(--primary) vs var(--accent) for color-mix backgrounds | Use `color-mix(in srgb, var(--primary) 12%, transparent)` for dashboard chip backgrounds — NOT var(--accent) | The teacher dashboard does not set a custom --accent CSS variable (unlike the [slug] profile page which overrides it with teacher.accent_color). Dashboard uses the default --accent: oklch(0.97 0 0) which is near-white and would produce invisible chip backgrounds. The brand primary (var(--primary) = #3b4d3e) provides the correct green tint for dashboard contexts. The D043 accent-color pattern is only appropriate on the teacher profile page where --accent is overridden per-teacher. | No | agent |
| D051 | M011/S04 | convention | Dashboard border radius: rounded-xl everywhere vs mixed rounded-lg/rounded-xl | Upgrade all dashboard card surfaces to rounded-xl consistently | The Analytics page already uses rounded-xl for stat cards. S01 profile reviews use rounded-xl. S02 SessionTypeSelector uses rounded-xl. The dashboard currently mixes rounded-lg (older components) and rounded-xl (newer). Upgrading all to rounded-xl eliminates visual inconsistency and aligns with the premium bar set by S01/S02. | No | agent |
| D052 | M011/S04 | convention | Settings page container: how to add consistent width without double-padding | Add max-w-3xl mx-auto wrapper ONLY (no p-6) to avoid double-padding with AccountSettings' internal p-6 | AccountSettings internally renders with space-y-6 p-6. Adding p-6 at the page level would create double-padding (12px outer + 24px inner). The other settings sub-components (CapacitySettings, SessionTypeManager, SchoolEmailVerification) also have their own internal padding. Adding max-w-3xl mx-auto constrains width consistently with other dashboard pages without duplicating the padding layer. | Yes | agent |
| D053 | M012 | performance | Caching strategy for public profile page /[slug] | ISR (Incremental Static Regeneration) with on-demand revalidation via revalidatePath | Profile page content changes rarely (teacher bio, subjects, banner). ISR serves cached HTML from Vercel CDN (~50ms TTFB vs ~800ms dynamic SSR). On-demand revalidation via revalidatePath ensures teachers see their changes quickly. revalidatePath calls were already wired in profile.ts and bookings.ts — they become effective once ISR is added. Vercel Hobby ISR write limit (1,000/day) is well within range at current scale. | Yes — if teacher profile updates become frequent enough to make on-demand revalidation expensive | collaborative |
| D054 | M012 | performance | Caching strategy for /tutors directory page with dynamic search params | Short TTL revalidation (export const revalidate = 300, 5 minutes) on the base /tutors page | Full static generation is impossible because search params vary per request. Client-side filtering refactor would be too much churn. Short TTL revalidation (5 min) is zero-complexity and serves the base listing from CDN while accepting slightly stale filtered results. New teachers appear in directory within 5 minutes of publishing — acceptable for the current scale. | Yes — if real-time directory freshness becomes important or client-side filtering is implemented | collaborative |
| D055 | M012 | performance | Vercel runtime strategy — no edge runtime for app routes | Node.js runtime for all app routes; edge runtime only for the proxy middleware (src/proxy.ts) | Edge runtime was removed from dashboard pages after hitting Vercel Hobby's 1MB bundle size limit. The proxy (middleware) stays edge because it is tiny and benefits from running at CDN nodes. All app routes use Node.js runtime which has no bundle size limit issue on Hobby. Any future edge runtime addition requires explicit bundle size verification before deployment. | Yes — if upgrading to Vercel Pro where edge bundle limits are higher | human |
| D056 | M012/S01 | architecture | Preview mode implementation for ISR-cached profile page | Next.js draftMode() with shared secret token via /api/draft/[slug] endpoint | The profile page's ?preview=true searchParams check opts the entire route out of ISR caching (searchParams is a dynamic API in Next.js App Router without PPR). draftMode() is ISR-compatible: it bypasses Full Route Cache only for requests carrying the draft mode cookie, not for all visitors. A shared secret (DRAFT_MODE_SECRET) gates the endpoint — acceptable because unpublished pages already show "Page not available" to non-draft visitors. Lower complexity than per-session signed JWTs. WizardStep3 onboarding preview URL updated to use the new endpoint. | Yes — can upgrade to per-session signed JWT if the shared secret becomes a concern | agent |
| D057 | M012/S01/T02 — profile page ISR conversion | architecture | ISR data fetching must use supabaseAdmin, not createClient() | Migrate all ISR-routed pages to supabaseAdmin for data fetching; do not use createClient() on ISR routes | createClient() unconditionally calls cookies() from next/headers, which is a dynamic API. Even at build time, when no auth cookies are present, Next.js detects the cookies() call and opts the entire route out of Full Route Cache, preventing ISR from working. This was discovered during T02 when the /[slug] page built as ƒ (Dynamic) despite having generateStaticParams and export const revalidate = 3600. Solution: switch all read-only queries to supabaseAdmin (service role key), which has no cookie dependency. This is safe for public read-only pages where all data is already public. | Yes | agent |
| D058 | M012/S01/T02 — profile page ISR conversion | architecture | Client components using useSearchParams() on ISR routes require Suspense wrapper | Wrap any client component calling useSearchParams(), useRouter.search, or similar dynamic search param APIs in a <Suspense> boundary when rendering on ISR-cached pages | Next.js prerender phase cannot statically render components that read useSearchParams() without knowing the query string. When Next.js tries to prerender an ISR page containing such a component, it throws an error unless the component is suspended. BookingCalendar on the /[slug] page uses useSearchParams() to read filter params; wrapping it in <Suspense> allows the page to prerender the static shell, with BookingCalendar rendering on the client after hydration. Suspense fallback can be empty (no spinner) since the component always renders fully after hydration. | Yes | agent |
