---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - supabase/migrations/0001_initial_schema.sql
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-02
  - ONBOARD-03
  - ONBOARD-04
  - ONBOARD-05
  - ONBOARD-06
  - ONBOARD-07
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
  - PAGE-07
  - PAGE-08
  - PAGE-09
  - PAGE-10
  - CUSTOM-01
  - CUSTOM-02
  - CUSTOM-03
  - CUSTOM-04
  - AVAIL-01
  - AVAIL-02
  - AVAIL-03
  - VIS-01
  - VIS-02
  - DASH-06

must_haves:
  truths:
    - "All tables exist: teachers, availability, bookings, reviews"
    - "RLS is enabled on every table with correct policies"
    - "teachers.timezone is TEXT NOT NULL (IANA timezone)"
    - "All timestamp columns use TIMESTAMPTZ (never bare TIMESTAMP or TIME)"
    - "bookings has UNIQUE constraint on (teacher_id, booking_date, start_time)"
    - "teachers has UNIQUE constraint on slug"
    - "Supabase Storage bucket 'profile-images' exists as public bucket"
    - "Booking status CHECK constraint enforces valid state machine values"
  artifacts:
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "Complete Phase 1 database schema: tables, indexes, RLS policies, storage"
      contains: "CREATE TABLE teachers"
  key_links:
    - from: "teachers.user_id"
      to: "auth.users.id"
      via: "REFERENCES auth.users(id) ON DELETE CASCADE"
      pattern: "REFERENCES auth.users"
    - from: "availability.teacher_id"
      to: "teachers.id"
      via: "REFERENCES teachers(id) ON DELETE CASCADE"
      pattern: "REFERENCES teachers"
    - from: "RLS policies"
      to: "auth.uid()"
      via: "USING (auth.uid() = user_id)"
      pattern: "auth.uid"
---

<objective>
Create the complete Phase 1 database schema: teachers, availability, bookings (state machine stub), and reviews (stub) tables — with all RLS policies, indexes, timestamptz enforcement, IANA timezone column, booking unique constraint, and Supabase Storage bucket configuration.

Purpose: Every subsequent plan (onboarding wizard, public profile page, dashboard) reads and writes to this schema. Getting it right now prevents destructive migrations later.

Output: A single SQL migration file (0001_initial_schema.sql) that can be applied via `supabase db push` or directly in the Supabase SQL editor, plus a Supabase Storage public bucket for profile images.
</objective>

<execution_context>
@/Users/soosupcha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/soosupcha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Complete schema — executor creates this exactly. -->

teachers table columns (all Phase 1 requirements mapped):
- id UUID PK
- user_id UUID → auth.users (AUTH-01, AUTH-02)
- slug TEXT UNIQUE NOT NULL (ONBOARD-07)
- full_name TEXT NOT NULL (ONBOARD-01, PAGE-02)
- school TEXT (PAGE-02)
- city TEXT, state CHAR(2) (PAGE-02)
- years_experience SMALLINT (ONBOARD-01, PAGE-03)
- subjects TEXT[] DEFAULT '{}' (ONBOARD-02, PAGE-03, PAGE-05)
- grade_levels TEXT[] DEFAULT '{}' (ONBOARD-03, PAGE-03)
- hourly_rate NUMERIC(10,2) (ONBOARD-06, PAGE-05)
- bio TEXT (PAGE-04 — auto-gen if null)
- photo_url TEXT (PAGE-02)
- banner_url TEXT (CUSTOM-04, PAGE-09)
- headline TEXT (CUSTOM-02, PAGE-08)
- accent_color TEXT DEFAULT '#3B82F6' (CUSTOM-01, PAGE-07)
- social_instagram TEXT (CUSTOM-03, PAGE-10)
- social_email TEXT (CUSTOM-03, PAGE-10)
- social_website TEXT (CUSTOM-03, PAGE-10)
- is_published BOOLEAN NOT NULL DEFAULT FALSE (VIS-01, DASH-06)
- timezone TEXT NOT NULL DEFAULT 'America/New_York' (ONBOARD-04, AVAIL-03)
- wizard_step SMALLINT NOT NULL DEFAULT 1 (resume onboarding)
- stripe_account_id TEXT (stub for Phase 3)
- stripe_charges_enabled BOOLEAN NOT NULL DEFAULT FALSE (stub for Phase 3)
- created_at TIMESTAMPTZ DEFAULT NOW()
- updated_at TIMESTAMPTZ DEFAULT NOW()

availability table (AVAIL-01, AVAIL-02, AVAIL-03):
- id UUID PK
- teacher_id UUID → teachers (CASCADE)
- day_of_week SMALLINT NOT NULL CHECK (0-6) — 0=Sun, stored relative to teacher's IANA tz
- start_time TIME NOT NULL — stored in teacher's IANA timezone context
- end_time TIME NOT NULL
- UNIQUE(teacher_id, day_of_week, start_time)

bookings table (stub for Phase 2 + constraint for BOOK-04):
- id UUID PK
- teacher_id UUID → teachers (no cascade — preserve history)
- parent_id UUID → auth.users (nullable — guest bookings)
- parent_email TEXT NOT NULL
- student_name TEXT NOT NULL
- subject TEXT NOT NULL
- booking_date DATE NOT NULL
- start_time TIME NOT NULL
- end_time TIME NOT NULL
- status TEXT NOT NULL DEFAULT 'requested' CHECK (IN ('requested','pending','confirmed','completed','cancelled'))
- stripe_payment_intent TEXT (Phase 3)
- notes TEXT
- created_at TIMESTAMPTZ DEFAULT NOW()
- updated_at TIMESTAMPTZ DEFAULT NOW()
- UNIQUE(teacher_id, booking_date, start_time) — BOOK-04 double-booking prevention

reviews table (stub for Phase 5):
- id UUID PK
- booking_id UUID UNIQUE → bookings
- teacher_id UUID → teachers
- parent_id UUID → auth.users (nullable)
- rating SMALLINT NOT NULL CHECK (1-5)
- text TEXT
- created_at TIMESTAMPTZ DEFAULT NOW()

RLS requirements:
- teachers: public SELECT (anon can read for /[slug] page), authenticated INSERT own, authenticated UPDATE own, no DELETE
- availability: public SELECT, teacher INSERT/UPDATE/DELETE own (via teacher_id → teachers.user_id = auth.uid())
- bookings: teacher can SELECT/UPDATE own (where teacher_id in their teachers), parent can SELECT own (where parent_id = auth.uid()), INSERT allowed anon (Phase 2 guest booking)
- reviews: public SELECT, parent INSERT own (where parent_id = auth.uid()), no UPDATE/DELETE

Storage bucket:
- Name: profile-images
- Public: true (no signed URLs needed for profile photos)
- RLS on storage.objects: teachers can INSERT/UPDATE/DELETE to profile-images where auth.uid() is in the path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete initial schema migration</name>
  <files>
    supabase/migrations/0001_initial_schema.sql
  </files>
  <action>
    Create supabase/migrations/0001_initial_schema.sql with the complete Phase 1 schema. Write the SQL in this exact order:

    PART 1 — TEACHERS TABLE:
    - Create table with all columns from the interfaces section
    - Add unique constraint on slug
    - Add index on slug (idx_teachers_slug)
    - Add index on user_id (idx_teachers_user_id)
    - Enable RLS: ALTER TABLE teachers ENABLE ROW LEVEL SECURITY
    - Add policies:
      * "teachers_public_read": SELECT USING (true) — allows /[slug] page to work for anon users
      * "teachers_insert_own": INSERT WITH CHECK (auth.uid() = user_id)
      * "teachers_update_own": UPDATE USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)
      * "teachers_no_delete": DELETE USING (false)
    - Add trigger for updated_at auto-update (create function if needed):
      ```sql
      CREATE OR REPLACE FUNCTION update_updated_at()
      RETURNS TRIGGER AS $$
      BEGIN NEW.updated_at = NOW(); RETURN NEW; END;
      $$ LANGUAGE plpgsql;

      CREATE TRIGGER teachers_updated_at
        BEFORE UPDATE ON teachers
        FOR EACH ROW EXECUTE FUNCTION update_updated_at();
      ```

    PART 2 — AVAILABILITY TABLE:
    - Create table with columns from interfaces section
    - Add index on teacher_id (idx_availability_teacher)
    - Enable RLS
    - Add policies:
      * "availability_public_read": SELECT USING (true)
      * "availability_teacher_write": ALL USING (EXISTS (SELECT 1 FROM teachers WHERE teachers.id = availability.teacher_id AND teachers.user_id = auth.uid())) WITH CHECK (same condition)

    PART 3 — BOOKINGS TABLE:
    - Create table with all columns from interfaces section
    - Include UNIQUE(teacher_id, booking_date, start_time) — BOOK-04 critical
    - Add index on (teacher_id, booking_date): idx_bookings_teacher_date
    - Enable RLS
    - Add policies:
      * "bookings_teacher_read": SELECT USING (EXISTS (SELECT 1 FROM teachers WHERE teachers.id = bookings.teacher_id AND teachers.user_id = auth.uid()))
      * "bookings_parent_read": SELECT USING (parent_id = auth.uid())
      * "bookings_anon_insert": INSERT WITH CHECK (true) — Phase 2 guest booking; tighten in Phase 2
      * "bookings_teacher_update": UPDATE USING (EXISTS (SELECT 1 FROM teachers WHERE teachers.id = bookings.teacher_id AND teachers.user_id = auth.uid()))
    - Add updated_at trigger

    PART 4 — REVIEWS TABLE:
    - Create table with columns from interfaces section
    - Enable RLS
    - Add policies:
      * "reviews_public_read": SELECT USING (true)
      * "reviews_parent_insert": INSERT WITH CHECK (parent_id = auth.uid())

    PART 5 — STORAGE BUCKET:
    Use Supabase storage bucket creation SQL (if supported) OR add a comment explaining the manual step:
    ```sql
    -- Storage bucket must be created manually OR via Supabase CLI:
    -- supabase storage create profile-images --public
    -- OR in Supabase Dashboard: Storage > New bucket > "profile-images" > Public: ON
    --
    -- Storage RLS (add in Supabase Dashboard > Storage > profile-images > Policies):
    -- Allow authenticated INSERT: (auth.uid()::text) = (storage.foldername(name))[1]
    -- Allow authenticated UPDATE/DELETE: (auth.uid()::text) = (storage.foldername(name))[1]
    -- Allow public SELECT: true
    ```

    IMPORTANT constraints to double-check:
    - NO bare TIMESTAMP columns (only TIMESTAMPTZ)
    - NO bare TIMESTAMP without timezone on any column
    - teachers.timezone is TEXT NOT NULL (IANA string, not an enum)
    - bookings UNIQUE constraint: UNIQUE(teacher_id, booking_date, start_time)
    - bookings status CHECK includes all 5 states: requested, pending, confirmed, completed, cancelled
    - availability.start_time and end_time are TIME (no tz — these are interpreted relative to teacher's timezone column, which is the correct pattern for recurring weekly slots)
  </action>
  <verify>
    <automated>cd /Users/soosupcha/Projects/Tutelo && cat supabase/migrations/0001_initial_schema.sql | grep -E "(TIMESTAMPTZ|TEXT NOT NULL|UNIQUE|ENABLE ROW LEVEL|CHECK)" | wc -l</automated>
  </verify>
  <done>
    File exists at supabase/migrations/0001_initial_schema.sql. Contains all 4 tables. RLS enabled on all tables. teachers.timezone is TEXT NOT NULL. bookings has UNIQUE(teacher_id, booking_date, start_time). All timestamp columns are TIMESTAMPTZ. Grep for TIMESTAMPTZ returns at least 6 matches. Grep for ENABLE ROW LEVEL SECURITY returns 4 (one per table).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply schema to Supabase and verify RLS policies</name>
  <files>
    supabase/migrations/0001_initial_schema.sql
  </files>
  <action>
    Apply the migration to the live Supabase project. Choose the method available:

    Method A — Supabase CLI (preferred):
    ```bash
    # Ensure Supabase CLI is installed
    npm install -g supabase
    # Login and link project
    supabase login
    supabase link --project-ref YOUR_PROJECT_REF
    # Push migration
    supabase db push
    ```

    Method B — Direct SQL (if CLI not available):
    Copy the entire contents of 0001_initial_schema.sql and execute in Supabase Dashboard > SQL Editor > New Query.

    After applying:
    1. Verify all 4 tables exist: Supabase Dashboard > Table Editor should show teachers, availability, bookings, reviews
    2. Verify RLS is enabled: Each table should show "RLS Enabled" badge
    3. Verify policies exist: Click each table > RLS policies tab — should show the policies defined in Task 1
    4. Create the Supabase Storage bucket:
       - Supabase Dashboard > Storage > New bucket
       - Name: profile-images
       - Toggle: Public bucket ON
       - Click Create bucket
    5. Run a quick RLS sanity check from the browser (after completing Plan 02 auth):
       - Open browser devtools console on http://localhost:3000
       - Run: `const sb = supabase.createBrowserClient(...); const { data } = await sb.from('teachers').select('*').limit(1); console.log(data)`
       - Should return empty array (not an error) — confirming public read RLS works

    Update 0001_initial_schema.sql with any corrections discovered during apply (SQL syntax issues, etc.).
  </action>
  <verify>
    <automated>cd /Users/soosupcha/Projects/Tutelo && cat supabase/migrations/0001_initial_schema.sql | grep "CREATE TABLE" | sort</automated>
  </verify>
  <done>
    Migration applied to Supabase (or SQL editor used). All 4 tables visible in Supabase Dashboard Table Editor. RLS enabled on all 4. profile-images storage bucket exists as public. 0001_initial_schema.sql committed to git.
  </done>
</task>

</tasks>

<verification>
1. `grep "CREATE TABLE" supabase/migrations/0001_initial_schema.sql` shows 4 tables
2. `grep "ENABLE ROW LEVEL SECURITY" supabase/migrations/0001_initial_schema.sql` shows 4 occurrences
3. `grep "TIMESTAMPTZ" supabase/migrations/0001_initial_schema.sql` shows at least 6 occurrences
4. `grep "UNIQUE(teacher_id, booking_date, start_time)" supabase/migrations/0001_initial_schema.sql` shows 1 match
5. `grep "TEXT NOT NULL" supabase/migrations/0001_initial_schema.sql | grep "timezone"` shows timezone column
6. No TIMESTAMP column without TZ: `grep -E "TIMESTAMP[^Z]" supabase/migrations/0001_initial_schema.sql` should be empty or only hit SQL keywords not column types
7. Supabase Dashboard: all 4 tables exist with RLS enabled
8. Supabase Storage: profile-images bucket is public
</verification>

<success_criteria>
- Single migration file with complete Phase 1 schema
- All 4 tables: teachers, availability, bookings, reviews
- RLS enabled on all tables with correct policies
- teachers.timezone TEXT NOT NULL (IANA)
- bookings UNIQUE(teacher_id, booking_date, start_time) for double-booking prevention
- All timestamps are TIMESTAMPTZ
- stripe_account_id and stripe_charges_enabled stubs on teachers (for Phase 3)
- wizard_step column on teachers (for onboarding resume)
- profile-images public Storage bucket created
- Migration applied to live Supabase project
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
