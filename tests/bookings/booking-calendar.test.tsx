import { describe, it } from 'vitest'

describe('BookingCalendar', () => {
  it.todo('renders subject dropdown when subjects.length > 1')
  it.todo('hides subject dropdown and auto-selects when subjects.length === 1')
  it.todo('shows inline success state after successful submit')
  it.todo("shows inline error 'slot_taken' message after failed submit with slot_taken error")
  it.todo("'Book another time' link resets to calendar step")
})
